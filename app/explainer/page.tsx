import { Placeholder } from "@/components/dev/Placeholder";

export default function ExplainerPage() {
  return (
    <Placeholder eyebrow="Explainer" title="Concept library" phase="Phase 12">
      <p className="text-sm text-muted-foreground">
        Fourteen concepts, each authored at three depths. Chips are coloured by
        mastery derived from actual in-game decisions. Works with no API key.
      </p>
    </Placeholder>
  );
}
